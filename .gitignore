node_modules/
dist/
packages/cli/web/
build/
.DS_Store
*.log
.env
.env.local
.inventarium/
# legacy state dirs — still present on machines that predate the rename,
# and must stay ignored or a `git add -A` sweeps thousands of replay files in.
.agent-trail/
.vibe-board/
*.db
*.db-shm
*.db-wal
.bun/
coverage/
.vscode/
.idea/
probe-output/

# Internal strategy docs — never public
docs/internal/
.worktrees/
~/
vibe-board.db*
